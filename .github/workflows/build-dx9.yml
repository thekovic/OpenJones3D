# CMake build workflow for DirectX 9
name: DirectX 9 Build

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false

      matrix:
        os: [windows-latest]
        build_type: [Release]
        #c_compiler: [gcc, clang, cl]
        c_compiler: [cl]
        qol_improvements: [ON, OFF]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
          # - os: ubuntu-latest
          #   c_compiler: gcc
          #   cpp_compiler: g++
          # - os: ubuntu-latest
          #   c_compiler: clang
          #   cpp_compiler: clang++
        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          # - os: ubuntu-latest
          #   c_compiler: cl

    steps:
    - uses: actions/checkout@v4

    - name: Set reusable strings
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Install DirectX 9 SDK (June 2010)
      run: |
        choco install directx-sdk -y
    
    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x86
    
    - name: Get DirectX 9 SDK Paths
      id: dx9_path
      shell: pwsh
      run: |
        $DX_SDK_ROOT = "C:\Program Files (x86)\Microsoft DirectX 9.0c SDK"
        $DX_INCLUDE = "$DX_SDK_ROOT\Include"
        $DX_LIB = "$DX_SDK_ROOT\Lib\x86"
        
        Write-Host "DirectX 9 SDK Root: $DX_SDK_ROOT"
        Write-Host "DirectX Include: $DX_INCLUDE"
        Write-Host "DirectX Lib: $DX_LIB"
        
        echo "DX_INCLUDE=$DX_INCLUDE" >> $env:GITHUB_OUTPUT
        echo "DX_LIB=$DX_LIB" >> $env:GITHUB_OUTPUT

    - name: Configure CMake (QOL ${{ matrix.qol_improvements }})
      shell: pwsh
      run: |
        cmake -B "${{ steps.strings.outputs.build-output-dir }}" `
          -A Win32 `
          -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }} `
          -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} `
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} `
          -DCMAKE_CXX_FLAGS="/I `"${{ steps.dx9_path.outputs.DX_INCLUDE }}`"" `
          -DCMAKE_EXE_LINKER_FLAGS="/LIBPATH:`"${{ steps.dx9_path.outputs.DX_LIB }}`"" `
          -DJONES3D_USE_DIRECTX9=ON `
          -DJONES3D_QOL_IMPROVEMENTS=${{ matrix.qol_improvements }} `
          -DJONES3D_ENABLE_POST_BUILD_COPY=OFF `
          -S "${{ github.workspace }}"

    - name: Build
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}
